<%= form_for(@low_value_consumption_inventory) do |f| %>
  <% if @low_value_consumption_inventory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@low_value_consumption_inventory.errors.count, "error") %> prohibited this low_value_consumption_inventory from being saved:</h2>

      <ul>
      <% @low_value_consumption_inventory.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
