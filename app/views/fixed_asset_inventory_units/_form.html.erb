<%= form_for(@fixed_asset_inventory_unit) do |f| %>
  <% if @fixed_asset_inventory_unit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fixed_asset_inventory_unit.errors.count, "error") %> prohibited this fixed_asset_inventory_unit from being saved:</h2>

      <ul>
      <% @fixed_asset_inventory_unit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
