<%= form_for(@fixed_asset_info) do |f| %>
  <% if @fixed_asset_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fixed_asset_info.errors.count, "error") %> prohibited this fixed_asset_info from being saved:</h2>

      <ul>
      <% @fixed_asset_info.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
