<%= form_for(@low_value_consumption_info) do |f| %>
  <% if @low_value_consumption_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@low_value_consumption_info.errors.count, "error") %> prohibited this low_value_consumption_info from being saved:</h2>

      <ul>
      <% @low_value_consumption_info.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
