<%= form_for(@rent_inventory_detail) do |f| %>
  <% if @rent_inventory_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rent_inventory_detail.errors.count, "error") %> prohibited this rent_inventory_detail from being saved:</h2>

      <ul>
      <% @rent_inventory_detail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
